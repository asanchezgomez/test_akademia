<h1>{{ test.name }}</h1>
<h4>results.html</h4>

<!-- Show the answers selected by the user and the result-->
<ol>
	{% for key, value in dic_answers_user.items %} 
	  <li>{{value.0}}</li>
	  <ul>
		{% if value.2 == True %}
			<li><b>Ha seleccionado "{{value.1}}" y es correcto</b></li>
		{% else %}
			<li>Ha seleccionado "{{value.1}}" y es incorrecto</li>
			<li>La respuesta correcta es "{{value.3}}"</li>
		{% endif %}
	  </ul>
	{% endfor %}
</ol>
<!-- Show the test's result -->
<h4>Resultado del test: {{ correct_answers }}/{{ num_questions }}</h4>
