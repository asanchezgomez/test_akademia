{% extends 'exam/base.html' %}

{% block body %}

	<div id="section">
		<div id="select_tests">
			<h2>Select one of the available tests:</h2>
			<div id="tests">
				{% if sort_test_list %}
					<ul>
					{% for test in sort_test_list %}
						<li><a href="{% url 'exam:questions_test' test.id %}">{{ test.name }}</a></li>
					{% endfor %}
					</ul>
				{% else %}
					<p>No tests are available.</p>
				{% endif %}
			</div>
		</div>
	</div>
{% endblock %}
